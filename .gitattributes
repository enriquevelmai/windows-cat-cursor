# Normalize text line endings; keep scripts as they are checked in.
* text=auto eol=lf
*.ps1 text eol=crlf
*.cs  text eol=crlf

# Treat generated/binary assets as binary (no diff/merge, no eol munging).
*.exe binary
*.cur binary
*.ani binary
*.ico binary
*.png binary
*.zip binary
